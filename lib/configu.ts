import { getCompan } from "./cookiesConf"



export default async function Admins(){
    const dados = await fetch(``,{
        headers: {
          'Content-Type': 'application/json',
        //   'API-Key': process.env.DATA_API_KEY,
        },
      })
    
}